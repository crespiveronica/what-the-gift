<!-- Contenido principal -->
<div class="container">
  <div class="page-header text-center">
    <h3>John Doe <br><small>johndoe2013 | johndoe@gmail.com</small></h3>
  </div>

  <div class="tabbable tabs-left">
    <% if @user=="vendedor" %> 
      <%= render template: 'profile/seller' %>
    <% else %>
      <%= render template: 'profile/user' %>
    <% end %>
  </div>
</div>