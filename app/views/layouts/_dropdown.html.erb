<% if signed_in? %>

  <li id="fat-menu" class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
      <%= "#{current_user.full_name}" %>

  <% if current_user.class.to_s == 'User' %>
      <% if current_user.pending_requests.count > 0 %>
        <span class="badge badge-warning"><%= current_user.pending_requests.count %></span>
      <% end %>
      <b class="caret"></b>
    </a>
    <ul class="dropdown-menu">
      <li><%= link_to "Perfil", current_user %></li>
      <li class="divider"></li>
      <% if current_user.pending_requests.count > 0 %>
        <li><%= link_to "Solicitudes de amistad (#{current_user.pending_requests.count})", friend_requests_path %></li>
        <li class="divider"></li>
      <% end %>
      <li>
        <%= link_to "Cerrar sesión", signout_path, method: "delete" %>
      </li>
    </ul>
  </li>

  <% elsif current_user.class.to_s == 'Seller' %>
    </a>
    <ul class="dropdown-menu">
      <li><%= link_to "Perfil", current_user %></li>
      <li class="divider"></li>
      <li>
        <%= link_to "Cerrar sesión", signout_path, method: "delete" %>
      </li>
    </ul>

  <% elsif current_user.class.to_s == 'Admin' %>
    </a>
    <ul class="dropdown-menu">
      <li><%= link_to "Panel de control", admins_path %></li>
      <li class="divider"></li>
      <li>
        <%= link_to "Cerrar sesión", signout_path, method: "delete" %>
      </li>
    </ul>

  <% end %>

<% else %>
  <li><%= link_to "Iniciar sesión", signin_path %></li>
<% end %>