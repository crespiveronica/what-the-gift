<% content_for :activefriendwishlist do %>
	class="active"
<% end %>
<fieldset>
	<legend>Hacele un regalo!</legend>
	<ul class="thumbnails">
		<% @products.each_with_index do |product, i| %>
			<li class="span2">
			  <div class="thumbnail gift">
			    <%= image_tag "gift1.jpg", size: '100x100', class: 'img-rounded' %>
			    <h5><%= product.name %></h5>
			    <p><small><%= product.description %></small></p>
			    <p class="bottom">
			    	<%= link_to 'Ver más', product_path(:id => product.id), :class => 'btn btn-mini btn-primary pull-right' %>
			    </p>
			  </div>
			</li>
		<% end %>
	</ul>
</fieldset>