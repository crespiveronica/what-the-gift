<!-- Contenido principal -->
<div class="container">
  <div class="page-header text-center">
    <h3> <%= @user.full_name %> <br><small><%= @user.email %></small></h3>
  </div>

  <div class="container">
    <% if @user=="vendedor" %>
      <%= render template: 'users/edit_seller' %> -->
    <% else %>
      <%= render template: 'users/edit_user' %>
    <% end %>
  </div>
</div>

<%= render template: 'users/modals' %>